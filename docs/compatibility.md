# 兼容性与实现说明

轻响通过 Windows UI Automation 获取微信会话快照，再根据未读数变化触发提示音。本文记录 v0.2.0 的适配方式和验证范围，方便排查问题或继续开发。

## 支持环境

| 项目 | 当前支持 |
| --- | --- |
| 系统 | Windows 10 / 11 x64 |
| 运行环境 | .NET Framework 4.x |
| 微信最小化适配 | 4.1.13.63 |
| 微信模块 | `Weixin.dll` |
| 模块 SHA-256 | `8bf9ae1edf52676bbfa2e0c4024af207c0f9bba589758f99de467515c90635e1` |

版本号和文件校验共同确定适配对象。微信升级后，如果底部提示版本不支持，可以在 [Issues](https://github.com/kianbqh/WeChatChime/issues) 提供微信版本和助手状态文字，方便继续适配。

## 会话快照与提醒

读取线程找到微信主窗口后，定位 `session_list`，批量缓存会话元素的名称、控件标识和类型。解析器从 `session_item_` 标识提取会话名称，并解析摘要行开头的未读计数。每轮正常读取后等待约 1.8 秒；读取不可用时改为 5 秒后重试。

提醒引擎保存每个会话上一次的未读数。只有名称匹配、规则开启、未读数增加且超过冷却时间，才发出音效请求。首次读取建立基线，避免启动时补响旧消息；修改音效或暂停设置不会清空已有基线。进程、进程创建时间或主窗口改变时重新建立基线。

UIA 只提供微信当时暴露的会话，因此建议先置顶重点联系人。没有未读变化的当前聊天、已经封顶的未读计数，以及暂未出现在可读取列表中的会话，都可能无法触发提醒。识别到同名会话后会暂停该名称的规则，可设置不同备注名后重新打开助手。

## 最小化兼容模式

当前适配通过微信进程内的一个辅助访问状态字节，让会话控件树保持可读取。该字节位于模块 RVA `0xAE2B0C8`，关联指令位于 RVA `0x839902`。

启用前会校验版本和文件散列、主窗口归属、进程创建时间、模块范围、运行时指令与内存页权限。校验通过后，将原始值 `0` 改为 `1` 并回读确认，在监听期间保持启用。这是针对特定版本的非官方适配，修改发生在进程内存中。

关闭兼容模式或正常退出时，通过保留的原进程句柄还原自身修改的状态；原本就是 `1` 的状态保持不变。工具被强制结束时可能无法完成还原，此时重启微信即可重新初始化状态。新下载的程序默认关闭兼容模式，由使用者在界面中开启。

## 本地验证

`test.ps1` 包含 326 项检查：配置与音效 63 项、消息解析与提醒规则 27 项、兼容校验与生命周期 44 项、原生界面交互及布局 192 项。兼容校验使用测试数据，不操作真实微信；UI 检查包括程序化的 125% / 150% 缩放。

在微信 4.1.13.63 上已验证最小化状态下持续读取 9 个会话、关闭模式还原、正常退出还原、助手收进托盘后继续读取以及重复启动恢复窗口。真实联系人来信到指定音效的完整收信测试，以及微信重新登录、长期运行和实际系统 DPI，仍是后续验证项目。

完整程序一次 50 秒的空规则运行样本：Working Set 约 58–63 MB，累计 CPU 时间约 0.28 秒。此样本开启了最小化兼容，未播放音频；资源占用会随运行时间、会话数量和系统环境变化。

## 技术参考

- [Microsoft UI Automation 缓存机制](https://learn.microsoft.com/en-us/windows/win32/winauto/uiauto-cachingforclients)
- [pywechat 的微信 4.x 控件定义](https://github.com/Hello-Mr-Crab/pywechat/blob/main/src/pyweixin/Uielements.py)
- [微信 Qt accessibility 适配线索](https://github.com/fanyuantaier/wechatauto-replica/blob/main/wechatauto/uia_driver.py)

主程序实现见 [Monitor.cs](../src/Monitor.cs) 和 [CompatibilityAccess.cs](../src/CompatibilityAccess.cs)。
